
import React from 'react';
import ChatContainer from '@/components/chat/components/ChatContainer.tsx';

const Chat = () => {
  return (
    <div className="h-full bg-secondary/30">
      <ChatContainer />
    </div>
  );
};

export default Chat;
